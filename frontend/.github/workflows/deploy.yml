name: Auto Deploy Backend to KT Cloud

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 백엔드 서비스 타겟 배포
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd ~/iot-platform/frontend
            git pull
            
            cd ~/iot-platform
            
            sudo docker-compose rm -fs nginx
            
            sudo docker-compose up -d --build --no-deps nginx